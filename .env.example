# ═══════════════════════════════════════════════════════════════
# ZENITH — Environment Variables
# ═══════════════════════════════════════════════════════════════
# Copy this file as .env and fill in the values.
# NEVER commit the .env file to a public repository.
# ═══════════════════════════════════════════════════════════════

# ── Backend Configuration (Required) ──────────────────────────
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGci...your_anon_key_here
VITE_SUPABASE_PROJECT_ID=your_project_id

# ── External Customs APIs (Optional — have default values) ───
# VITE_SIGA_GATEWAY_URL=https://siga.ana.gob.pa/api/v1
# VITE_VUCE_URL=https://vuce.gob.pa/api/v1
# VITE_ANA_CONSULTAS_URL=https://consultas.ana.gob.pa/api

# ── Backend Secrets (configure in Cloud Dashboard) ────────────
# These variables do NOT use the VITE_ prefix (server-side only):
# LOVABLE_API_KEY=sk-...           → Backend functions (Stella AI)
# SUPABASE_SERVICE_ROLE_KEY=...    → Auto-configured
# NEXUS_SECRET_KEY=...             → Inter-system HMAC signing
# RESEND_API_KEY=re_...            → Email notifications (optional)

# ── Production Environment (Future) ──────────────────────────
# AWS_REGION=us-east-1
# AWS_COGNITO_POOL_ID=...
# AWS_RDS_CONNECTION_STRING=...
